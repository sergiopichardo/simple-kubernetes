apiVersion: v1
kind: Service 
metadata:  
  name: client-node-port
spec: 
  type: NodePort
  ports: 
    - port: 3050 
      # the container port where we are redirecting the request
      targetPort: 3000  
      nodePort: 31515
  selector: 
    component: web 

